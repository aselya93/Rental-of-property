const { User } = require("../db/models");

class AuthService {
    static async authenticateUser(email, password) {
        
    }

    static async authenticateUser(email, password) {
        
    }


}